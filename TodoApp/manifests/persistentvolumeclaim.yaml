apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: todoapp-claim
  namespace: todoapp
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi