apiVersion: v1
kind: Service
metadata:
  name: todoapp-frontend-svc
spec:
  type: ClusterIP
  selector:
    app-frontend: todoapp-frontend
  ports:
    - port: 3001
      protocol: TCP
      targetPort: 3000